exports.handler = async (event) => {
    // TODO implement
    const response = {
        statusCode: 200,
        body: JSON.stringify({
            message: "Are you sure uwu",
        }),
    };
    return response;
};
